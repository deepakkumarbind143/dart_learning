// final is exactly like JS const. You don't know the value yet (maybe it's coming from a database), but once it's set, it can never be changed.

void main() {
  int age = 10;
  double marks = 10.0;
  String hi = "hello";
  bool isthere = true;

  String name = "Gemini"; // Cannot be null
  String? nickname; // Can be null

  // list ,sets, maps
}
